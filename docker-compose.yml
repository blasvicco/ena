services:
  ena_experiment:
    build:
      context: ./
      dockerfile: hub/python/Dockerfile
    volumes:
      - ./app:/home/app
    command: ["tail", "-f", "/dev/null"]
    container_name: ena-experiment
    working_dir: /home/app
